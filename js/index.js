const WEATHER_API = "https://api.weather.gov/points/";
async function getWeather() {
    let zip = document.querySelector('.zip').value;
    let points = ZIPCODES[zip];
    // Write code here to fetch data and display on page
}